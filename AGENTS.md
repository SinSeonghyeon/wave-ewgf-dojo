# AGENTS.md

## 목적

모든 AI 에이전트(Claude Code, Codex, 기타)가 매 세션 지켜야 하는 최소 규칙과 문서 라우팅만 둡니다. 프로젝트 지식과 진행 상황은 `.agents/docs/`에서 필요할 때만 읽고, 같은 규칙을 여러 문서에 반복하지 않습니다. Claude Code는 `CLAUDE.md`(`@AGENTS.md` 한 줄)로 이 파일을 불러옵니다.

## 프로젝트 기본 정보

- 이름: 미시마 도장 (Mishima Dojo / 三島道場). 철권 미시마류 웨이브 대시·초풍 입력을 브라우저에서 프레임 단위로 판정하는 연습 도구. 비공식 팬 제작.
- 코드: 루트 `index.html` 단일 파일(HTML+CSS+JS). 빌드 없음, 외부 라이브러리 없음.
- 배포: GitHub Pages, main 브랜치 루트, 커스텀 도메인 https://mishimaryu.com/ (루트 `CNAME` 파일, Cloudflare Registrar에서 2026-09-13 구매, 자동 갱신 꺼짐 — 만료 전 사용자가 갱신 결정). 예전 주소 https://sinseonghyeon.github.io/wave-ewgf-dojo/ 는 GitHub이 새 도메인으로 301. 저장소 이름은 `wave-ewgf-dojo`로 유지하고, 도메인·`CNAME`·워커 `ALLOWED_ORIGINS`는 사용자 결정 없이 바꾸지 않는다.
- 주소 모음: 저장소 https://github.com/SinSeonghyeon/wave-ewgf-dojo · 백엔드 Worker https://mishima-dojo-board.mishima-dojo.workers.dev · 후원 Ko-fi https://ko-fi.com/misimadojo (en/ja) · 카카오페이 송금 링크 https://qr.kakaopay.com/Ej8EBCpJu (ko, 휴대폰 전용) · 문의 이메일 tlstjdgus3@gmail.com (푸터 mailto·README·LICENSE) — 코드의 `BOARD_URL`/`DONATE`와 같아야 한다.
- 테스트: `node --test tests/dojo.test.cjs tests/board.test.cjs` (Node 22.13+, 의존성 없음. 가짜 D1이 `node:sqlite`를 쓴다) · `node tests/smoke-chrome.js` (로컬 Chrome/Edge 헤드리스, CDP).
- 개발 도구: `tools/` (커밋 대상). `tools/cdp.js`는 헤드리스 브라우저 공용 모듈, `tools/make-og.js`는 `og.png` 재생성. 앱 자체는 여전히 `index.html` 하나다.
- 백엔드: `worker/` (Cloudflare Worker + D1: 누적 순위·방문자 수·한마디 게시판). 배포는 `npx wrangler deploy`(`worker/README.md`). 앱은 `fetch`로만 호출하며 `index.html`의 `BOARD_URL`이 비어 있으면 백엔드 UI 전체가 숨겨진다.
- 사용자: 한국어. 철권 플레이어. C++·Unity 경험, 웹은 익숙하지 않음. 설명은 간결하게, 용어는 격투게임 표기(6N23, d/f, 저스트) 그대로.

## 문서 라우팅

- 진행 상황·다음 작업·수익화 계획: `.agents/docs/PLAN.md`. 작업 시작 전 체크박스와 진행 로그를 본다.
- 코드 구조(상태 머신, i18n, 기록 저장 형식, 검증 절차): `.agents/docs/CODE_MAP.md`. 코드를 고칠 때만 읽는다.
- 과거 코드 리뷰 결과: `.agents/docs/reviews/`. 홍보 글 초안(1-6): `.agents/docs/PROMO.md`.
- 여러 기능 브랜치를 한 번에 합류할 때: `.agents/docs/MERGE_PROCESS.md`. 각 기능 브랜치는 `.agents/handoffs/<브랜치명>.md`를 하나씩 커밋하고, 통합 담당자는 내용을 반영한 뒤 `main` 합류 직전에 `.agents/handoffs/`의 임시 문서를 모두 삭제한다.
- 사람용 소개·조작법: 루트 `README.md`. 에이전트 규칙은 넣지 않는다.
- 긴 파일은 `rg`로 위치를 좁힌 뒤 필요한 구간만 읽는다. `index.html`은 1,300줄이 넘는다.

## 설계 결정 (사용자 결정. 바꾸려면 먼저 물을 것)

1. 2026-09-18: 선행 중립 생략 `623`도 모든 기존 웨이브·초풍 경로에서 허용한다. 웨이브 표기는 `6N23 6 N 6N23 6 N …`. 3 직후 6은 후딜레이 캔슬용이고 다음 대시는 별도의 시작 6이 필요하다. 캔슬 6 없이 다음 6N23이 오면 "캔슬 6 누락", 캔슬 6을 시작 6으로 겸용하면 "시작 6 누락".
2. 초풍(2026-09-18 사용자 변경): 브라우저 공통 60Hz 격자에 각 입력 시각을 `floor(t/(1000/60)+0.5)`로 반올림해 대각·RP가 같은 칸일 때만 성공. 원점은 performance.timeOrigin, 화면 주사율과 무관. 간격 반올림과 구별한다. 0.5/0.7/0.9f 선택 UI 제거. 옛 store.window/Worker win은 저장·통신 호환용으로만 유지하며 새 초풍 판정에는 쓰지 않는다. 기존 기록은 유지, 순위의 판정 폭 열은 제거. 사범 피부 업적은 기존 진행 유지 + 동일 프레임 성공 중 원시 간격 ±8ms 이내를 센다.
3. 철권 공식 캐릭터 이름·그림을 쓰지 않는다. 자체 SD 도트와 자체 제작 리소스가 원칙이며, 2026-09-19 사용자가 명시적으로 추가한 BGM 곡들은 결정 8의 예외다. (수익화 시 IP 리스크 회피)
4. 단일 파일, 외부 라이브러리 없음. 차트는 직접 SVG 문자열. 런타임 외부 자원은 Google Fonts와 백엔드 Worker(`worker/`: 누적 순위·방문자 수·한마디, `fetch`만 사용, `BOARD_URL`이 비면 UI 숨김. 2026-09-12 사용자 결정)뿐. 다른 엔드포인트를 늘리려면 먼저 묻는다. 검색용 랜딩 `/en/index.html`·`/ja/index.html`(2026-09-13 사용자 요청)은 JS 없는 정적 HTML 두 장이고 앱 본체는 여전히 `index.html` 하나다. 랜딩은 `../?lang=en|ja`로 앱을 열며 hreflang 4종(ko/en/ja/x-default)은 세 페이지와 `sitemap.xml`이 같아야 한다(테스트가 잡는다). 랜딩 문구도 결정 3(공식 캐릭터 이름 금지)을 따른다.
5. 다크 단일 테마. 색 토큰은 `:root`에만 정의. 2026-09-19 사용자 승인 G 시안: A의 먹색·목재색·붉은 강조 + C의 넓은 플레이 화면. 같은 날 후속 요청으로 화면 높이·캐릭터 배율을 줄이고 배경은 native WebGL 3D 도장(바닥 평면·벽·기둥·보·조명)으로 변경했다. 목재/회벽 재질은 index.html 내장 WebP 아틀라스, 외부 라이브러리·엔드포인트 없음. 2D SD 캐릭터·판정 구조는 유지하고 3D 카메라의 바닥 투영을 캐릭터 발 위치에 맞춘다. WebGL 미지원·컨텍스트 유실은 같은 원근의 Canvas 배경으로 대체하고 복구 시 GPU 자원을 다시 만든다. 입력 기록은 씬뷰 왼쪽 고정 세로 목록(최신 위, 최대 40개·화면 높이만큼 표시하고 나머지는 내부 스크롤), 방향은 유지 프레임·버튼은 직전 입력 간격을 표시한다. 설정·옷장·보상은 우상단 가로 배치, 통계·코치는 스테이지 아래, 상세 분석 다음에 순위를 둔다. 2026-09-19 후속 요청: 한마디는 플레이 오른쪽(1100px 미만은 바로 아래), 웨캔기어 타임라인·A/B 평가는 모든 화면에서 플레이 아래로 통합한다. 측정 시작·웨캔기어 도전 버튼은 1P/2P 왼쪽, 도전 카운트다운·타이머는 씬 중앙 상단, 자유 연습은 즉시 입력 유지. 세션 초기화 버튼은 제거하되 설정의 기록·업적 초기화는 유지한다. 2026-09-19 후속 사용자 요청: 한마디의 글 사이 여백을 줄이고 짧은 글 약5개가 보이도록 게임 씬과 패널 높이를 함께 늘린다. 데스크톱 씬465~510px·한마디520~565px, 모바일 한마디520px. 본문·답글은 생략하지 않고 내부 스크롤로 전부 유지한다.
6. 앱 이름은 "미시마 도장". 영어 Mishima Dojo, 일본어 三島道場.
7. 사용자에게 보이는 모든 문자열은 `I18N` 사전(ko/en/ja)을 거친다. 새 문자열은 세 언어에 같은 키로 추가하고, 동적 문구는 `[key, ...args]` 배열이나 클로저로 저장해 언어 전환 시 다시 그린다. 테스트가 키 누락을 잡는다.
8. 소리(2026-09-12): BGM은 첫 키·클릭 뒤 재생, 효과음은 완료된 크라우치 대시·초풍 성공 및 사용자 추가 요청(2026-09-18)의 통발·나락·웨캔기어 기술 성공 및 실제 백대시 출력에 재생한다(2026-09-18 정리 음원 교체·백대시 추가). 피격음 원본의 강한 타격음이 정리본에서는 약120ms 뒤에 시작하므로 SFX_START.hit=0.12로 저레벨 앞부분을 건너뛴다(원본 파일 유지, 2026-09-18 디코딩 파형 확인). 첫 사용자 입력에서 모든 효과음 풀을 미리 불러온다. 피격음은 사거리·타입이 맞는 더미의 피격 반응 시작(명중 판정 후 기술별 160~200ms, 사이트 연출 기준)에 1회만 재생한다. 2026-09-18 사용자 조정: 통발·기상어퍼 음량은 효과음 설정의 80%로 재생하며 볼륨 변경 후에도 유지한다. 기술 소리는 헛쳐도 재생하지만 피격음은 헛침·중복 명중에는 없다. 소리는 연출 끄기·동작 줄이기와 독립이고 효과음 볼륨/음소거를 따른다. 설정은 켜기/끄기 + 배경음·효과음 볼륨 분리. 부트 시 Audio를 만들지 않는다(테스트·og 생성이 미디어를 안 건드리게). 배경음만 끄는 토글 `store.bgm`이 따로 있고(2026-09-14, 설정 창 + 헤더 스피커 버튼 `#bgmBtn`), 헤더 버튼은 `sound && bgm` 효과 상태를 보여준다. 배경음을 켤 때 마스터 소리도 켜며 효과음 볼륨은 유지한다. 2026-09-19 사용자 요청: `bgm/` 전용 폴더의 MP3를 랜덤 순환한다. 현재 bgm.mp3 + 사용자 지정 High Rollers Club + DUOMO DI SIRIO + Mishima DOJO이며 Moonlit Wilderness는 제외했다. 기존 main/root Pages의 Jekyll이 `bgm/playlist.json`을 자동 생성하고 앱은 같은 출처에서 읽는다(외부 API 추가 없음). 로컬 미리보기 목록은 `node tools/update-bgm.js`로 갱신한다. 하위 폴더·숨김 파일 제외, 파일명을 곡명으로 표시한다. 빈 폴더는 무음, 한 곡은 반복한다. 곡 종료·다음 곡 선택 때 직전 곡은 제외하고 store.bgmLast에 파일 경로를 저장해 목록 순서 변경·재접속 때도 제외한다. 상단 음소거/재생·일시정지/다음 곡/현재 곡을 한 그룹으로 묶는다. 일시정지는 snd.paused 세션 상태로 유지하며 음소거·효과음과 분리, 다음 곡도 일시정지·음소거를 해제하지 않는다. 기존 Web Lock 단일 창 재생과 백그라운드 중지는 유지한다. 결정 8 음량 보정 추가(2026-09-19): 기본 bgm/bgm.mp3의 -19.17 LUFS를 기준으로 BGM_GAIN 경로별 재생 게인을 적용한다. MP3 원본은 보존, 설정 볼륨에 곱하고 효과음은 유지. tools/measure-bgm.js가 FFmpeg loudnorm 입력 측정으로 게인을 재생성하며 조용한 곡은 증폭하지 않는다. 새 파일의 자동 목록 등록과 음량 측정은 별도다.
9. 걷기·대시·백대시·연속 초풍 팝(2026-09-12)은 연출 전용이며 판정 상태 머신과 통계를 바꾸지 않는다(백대시만 2026-09-13부터 자유 연습·백대시 10초에서 별도 `bd` 머신이 판정한다 — 결정 17. b,N,b 연출 자체와 `cd`는 그대로). 대초는 기존 f,N,f,N,d,df+2 판정에 라벨만 붙인 것이다. 웨이브의 캔슬 6 → N → 시작 6은 그 시작 6을 대시로 취급한다(2026-09-13 사용자 결정으로 변경. 캔슬 6 자체는 대시 아님. 연출은 짧은 대시이고 대초 라벨은 붙지 않는다 — `cd.dashWave`).
10. 라이선스(2026-09-12): 오픈소스가 아니다. 루트 `LICENSE`(모든 권리 보유, 복제·수정·재배포 금지), 푸터 `footer.copy`, `index.html` 첫 줄 주석으로 표기. MIT 등 오픈소스 라이선스를 붙이지 않는다. 백엔드 Worker는 `ALLOWED_ORIGINS`로 사이트 출처만 허용하며, 도메인이 바뀌면 `worker/wrangler.toml`을 고치고 다시 배포한다.
11. 터치 컨트롤(2026-09-12, 방향 버튼 방식·크기 범위는 2026-09-15, 기본 크기 자동 조절은 2026-09-16 사용자 변경): ←·↓·→ 독립 버튼 + 1 2 / 3 4 버튼을 스테이지 안 오버레이로, `pointer: coarse` 터치 기기에서만 자동 표시(설정에서 강제 가능). ↓+좌/우 동시 입력으로 대각선을 만들고, 설정에서 방향 버튼 크기(70~300%, 기본 100%)·가로/세로 위치를 조절한다. 새 100%의 실제 크기는 종전 140%와 같되 좁은 화면에서는 공격 버튼 앞 폭에 맞춰 자동 축소한다. 100% 이외의 사용자 지정 크기는 강제 축소하지 않아 공격 버튼과 겹칠 수 있고, 가로 위치는 공격 버튼과 무관하게 오버레이 안에서 움직인다. 입력은 키보드·패드와 같은 `recomputeDir`/`onButton` 경로를 타며 판정을 바꾸지 않는다. 터치 기록도 같은 순위 보드에 표시 없이 올린다. 정밀도가 낮다는 문구를 오버레이와 README에 둔다.
12. 통발(f,f+2)·나락(6N23+4)(2026-09-13): 초풍 판정과 분리된 `strike` 경로다. `attempt`가 아니므로 초풍 성공률·히스토그램·연속 초풍·`session.tries/hits`에 잡히지 않는다. 나락은 저스트가 없어 3(d/f)만 들어가면 인정한다. 둘 다 저스트가 아니므로 라벨에 "!"나 연속 횟수를 붙이지 않는다(초풍과 구분).
13. 더미 격파 30초(rush30, 2026-09-13, 점수 2026-09-15 변경): 4번째 측정 모드이자 순위 4번째 보드. 상/중/하단 더미가 무작위로 나오고 상단=초풍, 중단=통발, 하단=나락으로 맞으면 10점(저스트를 놓친 풍신권이 상단에 닿으면 5점), 크라우치 대시는 어디서든 연속 수만큼(최대 3점). 틀린 기술은 헛침(0점, 더미 유지). 격파(초풍·통발·나락)해도 웨이브 콤보(cd.chain)는 끊기지 않는다(2026-09-13 사용자 요청, `endCommand`가 rush에서 chain 유지). 700ms 공백에만 endChain으로 초기화된다. 워커 `BOARDS`에 rush30을 넣었고 **재배포는 사용자 작업**이다.
14. 결과 창 등급(2026-09-13): 측정 모드가 끝나면 순위 상위 %로 SS(≤1%)·S(≤10%)·A(≤20%)·B(≤50%)·C(≤70%)·D(그 외) 등급을 매긴다(`TIERS`/`tierOf`). 참가자 10명 미만은 10명으로 계산한다(혼자 1위 = S). 등급 문자는 세 언어 공통, 코멘트는 측정 모드마다 다르다(`tier.N.<mode>`, 톤은 SS "사람이 아니군요!?" / D는 자조 개그). 웨이브 속도 차트의 상위 띠·코치 "상위권 속도"는 고정 5 대시/초가 아니라 웨이브 10초 순위 상위 10% 경계 점수(워커 `/top`·`/submit`의 `cut10`, 10명 미만은 1위 점수)를 쓰고, 워커 응답이 없으면 5로 폴백한다. **워커 재배포는 사용자 작업**.
15. 옷장·업적(2026-09-13, 사용자 요청): 캐릭터 코스튬을 머리·상의·팔·하의·신발·피부 6슬롯으로 갈아입힌다(`#fitDlg`, 스테이지 톱니 아래 버튼). 세트 4벌(붉은 도복·뇌신·사범·마왕)은 철권 캐릭터 **오마주**이되 자체 디자인·자체 이름만 쓰고 캐릭터 이름은 코드·문구·문서 어디에도 쓰지 않는다(결정 3 유지, 테스트가 금지어를 잡는다). 업적 25개 = 아이템 25개 1:1(세트 4×6 + 후원 버튼을 눌러 후원 창을 연 "후원 생각이 있었군요..!?" → 밥그릇 투구). 출석 전용 아이템 12개는 KST 하루 첫 키·클릭에 무작위로 하나씩(`dailyGift`, `life.giftDay`). 획득 연출(`#jackpot`, 2026-09-13 사용자 수정: "?" 상자가 알 부화하듯 점점 세게 흔들리다 화이트아웃 → 결과 카드 페이드인, 광선·꽃잎 낙하, 배경 딤, 확인 버튼을 눌러야 닫힘. 소리는 Web Audio로 합성한 상승 차임 `playChime`, 음원 파일 없음). 설정에 기록·업적·복장 초기화 버튼(`#dataReset`, 닉네임·설정은 유지)이다. 2026-09-14 사용자 결정: 업적·출석은 `store.pendingRewards`에 미수령 보상으로 저장하고, 우측 상단 옷장 아래 보물상자(`#rewardOpen`, 금빛 맥동·개수 배지)를 눌렀을 때 전부 해금·저장한 뒤 대표 아이템 + "외 N개"로 기존 팝업을 한 번 재생한다. 수령 전 착용 불가, 새로고침 후에도 보상 유지, 수령 완료·미수령을 모두 제외해 중복 예약 방지. 측정·카운트다운·결과 연출·대화상자·수령 연출 중 수령 불가. 자동 팝업·자동 재개 없음(연출 중단 시 이미 수령한 아이템 유지). 보상 발생 시 작은 상단 토스트(`#rewardToast`, 업적/출석/여러 개)를 표시한 뒤 상자 쪽으로 축소 이동하고 상자가 반짝인다. 입력을 통과시키며 알림음은 없다. 효과 끄기·동작 줄이기는 정적 토스트·배지만. 초기화는 미수령 보상도 삭제한다. 진행도는 `store.life/ach/pendingRewards/fit`에 이 브라우저에만 저장하고 서버·워커는 건드리지 않는다. 판정 상태 머신·세션 통계·측정 점수는 읽기만 하며 바꾸지 않는다. 기본 복장은 기존 캐릭터와 픽셀 동일. 2026-09-19 재디자인은 사용자 요청으로 철회하고 기존 그림으로 복원했다. 공유 카드·og.png도 기존 캐릭터를 쓰며 손 번개 좌표 수정은 유지한다.
16. 순위 조작 대응(2026-09-13): 자동 치팅 판정은 넣지 않고 운영자가 **섀도 밴**한다(워커 `bans` 테이블, `tools/board-admin.js`). 차단된 닉의 기록은 계속 접수·저장되고 본인 브라우저에는 차단 전과 똑같은 순위표(자기 행·등수·참가 수 포함)가 보이지만, 남의 순위표·참가 수·`cut10`에는 빠진다. 앱 코드는 바꾸지 않는다(서버 응답만 달라짐). 2026-09-13 사용자 지시로 배포 완료. 이후 스키마 변경·재배포는 사용자 작업.
17. 백대시 연습·백대시 10초(bd10, 2026-09-13, 사용자 요청): 한국식 백대시 `414 N 414 N …`(4 N 4로 백대시, 1(↙)로 경직 캔슬, 1에서 4로 바로 굴려 다음 4 N 4). 판정 머신 `bd`는 `cd`와 **완전히 독립**(방향 입력만 봄 — 4·N·1이 세트, 2/8/7/9는 횡 캔슬, 3은 앉기 캔슬, 6은 실수 — 버튼과 `cd`는 읽지도 쓰지도 않음)이고 자유 연습과 bd10에서만 돈다(`bdActive`). 자유 연습에서는 첫 1 캔슬 전까지 문구를 내지 않는다(`bd.engaged`, b,N,b만으로는 위치 조정). 세트 등급은 참고 사이트식 입력별 홀드 프레임이 아니라 **세트 속도** = 이 백대시의 거리 ÷ (이 출력 → 다음 출력) m/s 하나로 매기고(`BD.TIERS`: 매우 빠름 ≥3.5 · 빠름 ≥3.0 · 보통 ≥2.2 · 느림), 캔슬 프레임·1 홀드·4N4 사이는 부제·코치와 "마지막 백대시 구간" 막대(`renderSeg`의 bd 변형: 4탭·N·4홀드·1)에 쓴다. 모델 상수 `BD`{MIN_F 6(그전에 1을 누르면 백대시 없음), MOVE_F 10(최대 거리 도달 = 최적 캔슬, 거리 = min(h,10)/10 m), RECOVER_F 26(백대시 경직: 그동안 뒤로만 못 간다 — 백대시·뒤 걷기 없음, 나머지는 전부 가능. 앉기·횡 방향이 지운다. 판정과 무관하게 **모든 모드의 연출**에 적용되며(`bdRec`), 경직 중엔 캐릭터가 백대시 자세를 유지하고 큰 텍스트는 띄우지 않는다), LINK_MAX_F 60}는 **실측이 아니라 사용자 체감 기반 가정값**이며 게임에서 확인 후 숫자만 바꾼다(문구·테스트가 상수를 읽는다). 1 뒤 중립(1→N→4)은 실수, 2/8 캔슬은 횡(가드 풀림) 실수로 표시하되 거리는 인정. 4 N 4를 묶는 규칙은 연출(`tapDetect`)과 같다(두 번째 4가 첫 4로부터 250ms 안). 캔슬 없이 4를 뗀 백대시의 1.0 m는 경직 26f 안에 앉기·횡이 오면 그 시점까지의 거리로 깎인다(떼고-앉기 반복으로 거리를 불릴 수 없음). 점수는 10초 동안 물러난 거리(m), tie = 매우 빠름 세트 수, 워커 `BOARDS.bd10` 추가 — **재배포는 사용자 작업이고 사이트 푸시보다 먼저** 하는 것을 권장(반대면 재배포 전까지 bd10 등록이 400으로 거부되고 순위 바에 실패 문구가 뜬다). 등급 이름은 Perfect/Great/Good을 쓰지 않는다.
18. 한마디 좋아요/싫어요(2026-09-13, 사용자 요청): 글마다 👍/👎 버튼과 수. 식별은 **닉네임+토큰**(브라우저 ID를 새로 만들지 않음) — 서버 `votes` 테이블 PK(post_id, 닉 key)로 닉네임당 글 하나에 표 1개, 닉이 없으면 닉 게이트. 취소(같은 버튼)·변경(반대 버튼) 가능하되 어느 순간에도 표는 최대 1개. `POST /vote {id, v:1|-1|0}`는 토글이 아니라 **멱등 "설정"**이라 낡은 화면·재전송에도 두 번 세지지 않는다. 내 표는 이 브라우저의 `store.votes`에만 기억하고, 누가 어디에 표했는지 돌려주는 API는 두지 않는다(`/posts?nick=`식 조회는 남의 표를 노출). 정렬은 최신순 그대로, 싫어요가 많아도 숨기지 않는다. 판정·순위·글쓰기 경로는 손대지 않았다. **스키마 적용·워커 재배포는 사용자 작업이며 사이트 푸시보다 먼저**(구 워커 + 새 사이트면 버튼이 0으로 보이고 누르면 실패 문구만).
19. 순위 초기화 없음(2026-09-14, 사용자 결정): 주간 초기화(월요일 0시 KST)를 없애고 순위는 **누적**이다. 닉네임당 보드마다 최고 기록 1건이 계속 남는다. 워커는 `scores.week`에 시즌 키 `SEASON`(`'all'`)만 쓰고 읽으며(`seasonKey()` 한 곳 — 유입이 늘어 초기화를 되살릴 때 여기서 `weekKey(now)`를 돌려주면 주간으로 돌아간다. 옛 키의 행은 지우지 않고 조회에서만 빠진다), `/top`·`/submit` 응답에서 `week/start/end`가 빠지고 `season`이 들어간다. 기존 주간 행은 `worker/migrate-2026-09-14-alltime.sql`로 닉·보드당 최고 1건만 남겨 `'all'`로 합친다(멱등). 앱은 순위 카드에 기간·초기화 문구 대신 참가 수만 보이고(`#boardInfo`), 웨이브 상위 띠 `cut10`은 최초·수동 순위 조회와 점수 등록·삭제 응답에서만 갱신하며 마지막 값을 만료 없이 유지한다. 문구에서 "주간"·"이번 주"를 뺐다(세 언어·랜딩·README). **워커 재배포 + 마이그레이션 실행은 사용자 작업**이며 순서는 재배포 → 마이그레이션(순서가 바뀌어도 마이그레이션을 한 번 더 돌리면 된다).
20. 공지사항(2026-09-15, 사용자 결정): `index.html`의 정적 `NOTICES`를 최신순으로 누적하고 헤더 종 버튼의 팝업에서 과거 공지까지 모두 보여준다. 최신 ID 미열람이면 `NEW` 배지. 첫 방문은 배지만 표시하고, 기존 방문자는 새 공지를 접속 후 한 번 자동으로 본다. 자동 공지는 측정·카운트다운·결과 연출·다른 대화상자를 중단하지 않고 안전할 때까지 기다린다. 읽음 ID `store.noticeSeen`은 브라우저에만 저장하며 백엔드는 쓰지 않는다. 새 공지는 ko/en/ja 키를 함께 추가한다.
21. 후원 유도(2026-09-15, 사용자 결정): 측정 모드 개인 최고 기록 결과창의 후원 안내는 KST 하루 1회만 보인다(첫 완주도 개인 최고). 본문은 한국어 `1,000원부터`와 도메인·서버 유지/기능 개발 용도를 구체적으로 쓰고, 기존 밥 농담은 작은 보조 문구로 남긴다. 실제 입력이 이어지는 연습 시간을 누적해 10분이 되면 헤더 후원 버튼이 8초 동안 금빛으로 빛나고 말풍선(`응애… 밥 줘… 🍚`)이 KST 하루 1회 뜬다. 입력 후 30초가 지나면 누적을 멈추며, 새로고침 뒤에도 그날의 시간·노출 여부를 유지한다. 측정·카운트다운·결과/설정/후원/옷장 창·보상 연출 중에는 말풍선을 띄우지 않고 끝난 뒤 표시한다. 동작 줄이기나 연출 끄기에서는 애니메이션 없이 정적으로 표시한다. 판정·통계·순위·워커는 바꾸지 않는다.
22. 한마디 대댓글(2026-09-14, 사용자 요청): 원글마다 1단계 대댓글을 여러 개 달 수 있다. 대댓글은 오래된 순으로 원글 아래에 전부 표시하고 재대댓글(2단계 이상)은 두지 않는다. 작성은 원글과 같은 닉네임+토큰 인증·1~200자 검증·`POST_LIMIT`(IP당 원글과 합쳐 1분 3개)을 쓴다. 워커 `POST /reply {id,nick,token,text}`, D1 `replies` 테이블이며 `/posts`가 각 원글에 `replies[]`를 포함한다. 원글 삭제 시 대댓글도 삭제된다. **스키마 적용·워커 재배포는 사용자 작업이며 사이트 푸시보다 먼저** 한다.

23. 본인 순위 기록 삭제(2026-09-14, 사용자 요청): 순위표의 본인 행에만 삭제 버튼을 보이고 확인 후 현재 보드의 본인 기록 1건을 지운다. 서버 `DELETE /score {board,nick,token}`가 닉네임+토큰 소유권을 검증하고, 행 ID는 클라이언트에서 받지 않으므로 다른 사람 기록은 삭제할 수 없다. 삭제해도 닉네임 점유·로컬 연습 기록은 유지되며, 그 보드의 측정을 다시 완주하면 새 점수가 자동 등록된다. **워커 재배포는 사용자 작업**이고 사이트 푸시보다 먼저 해야 한다.

24. D1 무료 한도·조회 절약(2026-09-15, 사용자 결정): Cloudflare D1 Free는 계정 전체 기준 하루 읽기 500만 행·쓰기 10만 행이며 00:00 UTC(09:00 KST)에 초기화된다. 한마디는 최초 로드·사용자 새로고침 버튼·글/답글/투표 변경 때만 조회하고 자동 폴링하지 않는다. 순위는 최초 로드·탭/새로고침·점수 등록/삭제 때만, 방문자는 최초 로드와 열린 탭의 KST 날짜 전환 때만 조회한다. 워커의 `/top`은 순위·상위 10·참가 수·내 행·`cut10`을 한 SQL로 구하고, `/posts`는 최신 50개를 먼저 제한한 뒤 투표를 집계한다. D1 변경 전후 `npx wrangler d1 insights mishima-dojo-board --timePeriod=1d --sort-type=sum --sort-by=reads --sort-direction=DESC --limit=20`로 실제 읽기 행을 확인하고, 인덱스를 타는 범위 조건·제한된 집계를 우선한다. 한도 초과는 워커가 503 `{error:'quota'}`로 구분하며 앱은 한마디에 무료 서버 한도와 후원 안내를 표시한다.

25. 웨캔기어(2026-09-18 사용자 승인·후속 UI 변경): 전용 `wsc` 모드에서 단계 버튼 없이 전체 입력을 평가하며 플레이 아래 패널에 A/B 피드백을 따로 표시한다. 선행 웨이브는 `6N23` 한 덩어리, 프레임 표는 마지막 대각을 1f로 시작. 최종 사용자 정정: A는 대각 입력 순간을 1f로 포함하므로 실제 7/8/9f 경과 → A=8/9/10, B=1/1~2/1~3f. A는 60Hz 경과 반올림 +1, B는 뒤 입력 순간 0f 기준 경과 반올림(유지). A는 대각 유지·중립·앞 입력 1회, B는 뒤 유지·중립만 허용. 뒤+RP 동시 실패. `6N23`·`623` 허용, 다음 완전한 웨이브의 마지막 대각으로 기준 갱신(캔슬·시작 앞은 별도). 세션 통계 하나 + 로컬 랜덤 과제 10회 도전(3초 카운트다운, 매 과제 N=0~3 무작위·중복 허용, 웨이브 N회 후 정확히 N+1번째에 성공, 성공률/최고 연속). 1초 중단·선행 순서 오류는 평가 분모 제외 및 같은 과제 재시도, 웨캔기어로 완료한 버튼 입력은 성공·실패 모두 다음 과제, 모드/방향/포커스/탭 숨김/모달 진입은 진행 시도·도전 취소. 완료 결과는 다음 도전까지 유지, 새로고침은 기록 삭제. 모든 모드가 동일 커맨드 판정·연출을 공유하며 자유 연습에서도 웨캔기어, wsc에서도 초풍·나락·통발 등을 쓸 수 있다. 대각 후보에 뒤 입력이 있으면 웨캔기어 경로가 버튼을 소비하고, 없으면 기존 기술로 판정한다. wsc 모드에서는 기존 기술의 통계·업적 기록도 차단하고 다른 기술 입력은 도전 과제를 소비하지 않는다. 다른 모드의 선행 웨이브는 기존 규칙대로 집계하되 웨캔기어 마무리는 초풍 시도·측정 점수·업적에 포함하지 않는다. 2026-09-19 사용자 추가 요청으로 전용 리더보드를 연결(결정 26), DB 스키마는 그대로. 하단 입력 프레임 이력 복구, 대각 이후 현재 칸 강조·A/B 실시간 숫자·모바일 자동 스크롤(2026-09-19 수정: 1~15f만 표시, 15f 초과 입력·현재 칸 강조는 생략하고 실제 A/B 숫자와 판정은 유지). 자체 기상어퍼(앉기→올려치기→복귀)·팝·일반 더미 반응 및 사용자 제공 기상어퍼 효과음. 도전 횟수만 틀린 경우에도 기술 자체가 성공하면 기술 연출·소리는 나오되 도전은 실패다. 명중 순간 180ms 충격 원·방사선·불꽃(기존 색 토큰)을 표시하며 연출 끄기·동작 줄이기에서는 생략한다. 실패 기술명 단정 금지. 상세 `.agents/docs/WSC_PRACTICE.md` 10·11절.

26. 웨캔기어 리더보드(2026-09-19 사용자 요청·점수 기준 확정): `wsc` 전용 보드에 랜덤 과제 10회 완주 결과만 자동 등록. score=성공 횟수(0~10 정수), tie=최고 연속 성공(0~10 정수). 동점은 기존 순위 정책대로 공동 순위. 중단/취소/자유 연습은 등록하지 않는다. `wsc.challenge.result`를 별도로 두며 기존 trial.result·로컬 측정 기록·업적·공유 카드에는 섞지 않는다. 실패 시 재시도, 닉네임 미설정 시 설정 후 등록, 본인 기록 삭제 지원. Worker BOARDS/validate와 관리자 도구에 추가. 기존 scores 테이블 사용으로 마이그레이션은 없고 **워커 재배포는 사용자 작업이며 사이트 푸시보다 먼저** 한다.

## 작업 원칙

- 요구가 모호하면 추측으로 진행하지 말고 해석안을 제시하거나 묻는다. 더 단순한 대안이 보이면 착수 전에 제안한다.
- 코드 변경 후 최소 검증: 단위 테스트 → 브라우저 스모크 테스트. 실패한 테스트는 결과 그대로 보고한다. 여기까지가 에이전트 몫이고, 커밋·푸시는 사용자가 한다(아래 "커밋·푸시는 사용자가" 참조).
- 기능을 끝내면 `.agents/docs/PLAN.md`의 체크박스에 `[x]`와 날짜, 진행 로그 한 줄을 남긴다. 설계 결정이 바뀌면 이 파일의 번호 목록을 고친다. 코드 구조가 바뀌면 `CODE_MAP.md`를 고친다.
- 여러 기능 브랜치를 묶어 배포할 예정이면 `.agents/docs/MERGE_PROCESS.md` 형식으로 자기 브랜치의 임시 인수인계 문서를 남긴다. 공지 초안, 선행 배포·마이그레이션, 충돌 예상 파일, 테스트 결과를 빠뜨리지 않는다.
- 작업을 마치면 마지막 보고에 **검증 안내**를 넣는다: 바뀐 파일 목록(`git status --short`), 사용자가 브라우저에서 직접 확인할 항목(무엇을 눌러서 무엇이 보여야 하는지), 실행한 테스트 결과, 그리고 그대로 붙여 넣을 수 있는 `git add <파일...>` + `git commit` + `git push` 명령. 커밋 메시지 초안도 함께 준다.

## 커밋·푸시는 사용자가 (2026-09-11 사용자 결정)

- 에이전트는 `git commit`, `git push`를 하지 않는다. 테스트를 통과시킨 작업 트리 상태로 두고 검증 안내를 보고하면 끝이다. 사용자가 직접 확인한 뒤 커밋·푸시한다.
- 예외: 사용자가 그 세션에서 "커밋해라", "푸시해라"라고 명시적으로 지시한 경우에만 한다. 그때도 파일을 명시해서 추가한다.
- Pages 반영 확인(`curl -s <배포 주소> | grep -o "<title>[^<]*</title>"`)은 사용자가 푸시했다고 알린 뒤에만 한다.
- 세션 시작 시 작업 트리에 미커밋 변경이 있으면 이전 세션의 검증 대기 작업일 수 있다. `git checkout`·`git reset`·`git stash`로 되돌리지 말고, 어떤 파일이 바뀌어 있는지 보고한 뒤 그 위에서 이어서 작업할지 사용자에게 묻는다.
- 미커밋 변경이 있는 상태에서 원격이 앞서 있으면 `git reset --hard origin/main`을 하지 않는다. 상황을 보고하고 사용자 지시를 기다린다.

## 파일·Git 안전

- 커밋은 파일을 명시해서 추가한다. `git add -A`, `git add .` 금지. 이 폴더에는 다른 세션이나 사용자가 파일을 수시로 떨어뜨린다(2026-09-11에 외부 mp3가 공개 저장소에 쓸려 들어가 히스토리를 재작성했다). `git status --short`에 낯선 파일이 있으면 커밋에 넣지 말고 사용자에게 묻는다.
- 같은 폴더에서 여러 에이전트 세션이 동시에 돌 수 있다. 시작할 때 `git status`, `git log origin/main..main`, `git log main..origin/main`으로 갈라짐을 확인한다. 로컬 main이 원격과 다르면 작업 트리 변경이 **없을 때만** `git fetch && git reset --hard origin/main`으로 맞춘다. 변경이 있으면 위 "커밋·푸시는 사용자가" 규칙대로 사용자에게 묻는다.
- 커밋하지 않을 임시 파일·실험 스크립트는 `.sandbox/<topic>/`(gitignore됨)에 둔다. 영속할 문서만 `.agents/docs/`에 둔다.
- 커밋과 `git push`는 사용자가 한다(위 절 참조). force push, 히스토리 재작성, 공개 저장소 생성, 저장소 설정·권한 변경은 사용자가 그 작업을 명시적으로 지시했을 때만 한다. 도구 권한이 막히면 필요한 명령을 안내하고 사용자가 `! <명령>`으로 실행하게 한다.
- 저작권이 의심되는 파일(게임 음원, 공식 이미지)은 커밋하지 않고 사용자에게 출처를 확인한다.
